当一个父亲类Father中写了虚函数Father::func()时，类中会存储一个vsprt（虚函数指针）指向vstable（虚函数表），表的内部存储虚函数的地址。如若子类Son重写（不是重载）虚函数，则子类中从父类中继承的虚函数表变成子类的虚函数表，表的内部存储Son::func(); 当父类的指针或者引用指向子类对象是，发生多态。如果父类的指针或引用指向的是子类的对象，那么将调用子类的虚函数的内容。